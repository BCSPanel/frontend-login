{
  "name": "web-login",
  "private": true,
  "version": "0.0.10",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "bview": "tsc && vite build && vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.13"
  },
  "dependencies": {
    "prettier": "^3.2.4",
    "rollup": "^4.9.6",
    "vite-plugin-mock-dev-server": "^1.5.0"
  }
}
